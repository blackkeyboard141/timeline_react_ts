interface UserEvent {
  id: number;
  title: string;
  dateStart: string;
  dateEnd: string;
}

const userEventReducer = (state, action) => {
  switch (action.type) {
    case value:
      break;

    default:
      return state;
  }
};

export default userEventReducer;
